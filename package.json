{
  "name": "domy-na-plazi",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "build:pages": "npm run fetch-featured && npm run fetch-offers && npm run build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch-offers": "node scripts/fetch-offers-feed.mjs",
    "fetch-featured": "node scripts/fetch-featured-property.mjs",
    "rename-flags": "node scripts/rename-flags.mjs"
  },
  "dependencies": {
    "astro": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fast-xml-parser": "^4.3.2",
    "typescript": "^5.0.0"
  }
}
